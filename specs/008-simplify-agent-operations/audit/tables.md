# Auditoria de tablas

Fuente: las 18 migraciones presentes en `database/migrations/` al 2026-07-23. Hay 17 tablas creadas; la migracion `000014` agrega `users.password_changed_at`. Los indices enumerados son los declarados por las migraciones (incluidos PK y UNIQUE), y todas las FKs usan `restrictOnDelete()`.

| Tabla | Columnas | FKs | Indices y restricciones |
|---|---|---|---|
| `organizations` | `id bigint`, `public_id char(36)`, `name varchar(160)`, `timezone varchar(64) default America/Lima`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | Ninguna | PK `id`; UNIQUE `public_id` |
| `users` | `id bigint`, `public_id char(36)`, `organization_id bigint`, `username_normalized varchar(100)`, `email_normalized varchar(254)`, `password varchar`, `password_changed_at datetime(6) nullable`, `role varchar(40)`, `status varchar(20) default ACTIVE`, `deactivated_at datetime(6) nullable`, `deactivated_by bigint nullable`, `deactivation_reason varchar(500) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `deactivated_by -> users.id` | PK `id`; UNIQUE `public_id`; UNIQUE (`organization_id`, `username_normalized`); UNIQUE (`organization_id`, `email_normalized`); INDEX (`organization_id`, `role`, `status`) |
| `auth_sessions` | `id bigint`, `public_id char(36)`, `user_id bigint`, `status varchar(20) default ACTIVE`, `started_at datetime(6)`, `access_expires_at datetime(6)`, `absolute_expires_at datetime(6)`, `last_refreshed_at datetime(6) nullable`, `ended_at datetime(6) nullable`, `end_reason varchar(40) nullable`, `ip_hash binary(32) nullable`, `user_agent_summary varchar(255) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `user_id -> users.id` | PK `id`; UNIQUE `public_id`; INDEX (`user_id`, `started_at`); INDEX (`user_id`, `status`, `started_at`); INDEX (`status`, `access_expires_at`); INDEX `absolute_expires_at` |
| `auth_refresh_tokens` | `id bigint`, `auth_session_id bigint`, `token_hash binary(32)`, `generation unsigned int default 1`, `state varchar(20) default ACTIVE`, `issued_at datetime(6)`, `expires_at datetime(6)`, `consumed_at datetime(6) nullable`, `revoked_at datetime(6) nullable`, `replaced_by_id bigint nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `auth_session_id -> auth_sessions.id`; `replaced_by_id -> auth_refresh_tokens.id` | PK `id`; UNIQUE `token_hash`; UNIQUE (`auth_session_id`, `generation`); INDEX (`auth_session_id`, `state`); INDEX `expires_at` |
| `session_events` | `id bigint`, `auth_session_id bigint nullable`, `user_id bigint`, `type varchar(40)`, `occurred_at datetime(6)`, `context json nullable`, `created_at datetime(6)` | `auth_session_id -> auth_sessions.id`; `user_id -> users.id` | PK `id`; INDEX (`auth_session_id`, `occurred_at`); INDEX (`user_id`, `occurred_at`); INDEX (`type`, `occurred_at`) |
| `audit_logs` | `id bigint`, `organization_id bigint`, `actor_user_id bigint nullable`, `action varchar(100)`, `entity_type varchar(100)`, `entity_id unsigned bigint`, `before_values json nullable`, `after_values json nullable`, `reason varchar(500) nullable`, `occurred_at datetime(6)`, `correlation_id char(36)`, `created_at datetime(6)` | `organization_id -> organizations.id`; `actor_user_id -> users.id` | PK `id`; INDEX (`entity_type`, `entity_id`, `occurred_at`); INDEX (`actor_user_id`, `occurred_at`); INDEX (`organization_id`, `occurred_at`); INDEX (`action`, `occurred_at`) |
| `regions` | `id bigint`, `organization_id bigint`, `name varchar(160)`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id` | PK `id`; UNIQUE (`organization_id`, `name`); INDEX (`organization_id`, `is_active`) |
| `provinces` | `id bigint`, `organization_id bigint`, `region_id bigint`, `name varchar(160)`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `region_id -> regions.id` | PK `id`; UNIQUE (`region_id`, `name`); INDEX (`organization_id`, `is_active`); INDEX (`region_id`, `is_active`) |
| `districts` | `id bigint`, `organization_id bigint`, `province_id bigint`, `name varchar(160)`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `province_id -> provinces.id` | PK `id`; UNIQUE (`province_id`, `name`); INDEX (`organization_id`, `is_active`); INDEX (`province_id`, `is_active`) |
| `stores` | `id bigint`, `organization_id bigint`, `district_id bigint`, `code varchar(80)`, `name varchar(200)`, `address varchar(500) nullable`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `district_id -> districts.id` | PK `id`; UNIQUE (`organization_id`, `code`); INDEX (`district_id`, `is_active`); INDEX (`organization_id`, `is_active`) |
| `banks` | `id bigint`, `organization_id bigint`, `code varchar(20)`, `name varchar(200)`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id` | PK `id`; UNIQUE (`organization_id`, `code`); INDEX (`organization_id`, `is_active`) |
| `bank_agents` | `id bigint`, `organization_id bigint`, `store_id bigint`, `bank_id bigint`, `code varchar(80)`, `terminal_code varchar(40) nullable`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `store_id -> stores.id`; `bank_id -> banks.id` | PK `id`; UNIQUE (`organization_id`, `code`); INDEX (`store_id`, `is_active`); INDEX (`bank_id`, `is_active`); INDEX (`organization_id`, `is_active`) |
| `user_bank_agent_assignments` | `id bigint`, `user_id bigint`, `bank_agent_id bigint`, `assigned_by bigint`, `assigned_at datetime(6)`, `unassigned_at datetime(6) nullable`, `is_active boolean default true`, `created_at datetime(6)`, `updated_at datetime(6)` | `user_id -> users.id`; `bank_agent_id -> bank_agents.id`; `assigned_by -> users.id` | PK `id`; INDEX (`user_id`, `is_active`); INDEX (`bank_agent_id`, `is_active`) |
| `operation_types` | `id bigint`, `organization_id bigint`, `bank_id bigint nullable`, `name varchar(160)`, `description varchar(500) nullable`, `cash_direction varchar(20)`, `is_active boolean default true`, `deactivated_at datetime(6) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `bank_id -> banks.id` | PK `id`; UNIQUE (`bank_id`, `name`); INDEX (`organization_id`, `is_active`) |
| `operations` | `id bigint`, `organization_id bigint`, `store_id bigint`, `bank_agent_id bigint`, `operation_type_id bigint`, `user_id bigint`, `amount decimal(18,2)`, `currency char(3) default PEN`, `effective_at datetime(6)`, `recorded_at datetime(6)`, `status varchar(20) default ACTIVE`, `reference varchar(100) nullable`, `observation varchar(500) nullable`, `annulled_by bigint nullable`, `annulled_at datetime(6) nullable`, `annulment_reason varchar(500) nullable`, `idempotency_key char(64)`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `store_id -> stores.id`; `bank_agent_id -> bank_agents.id`; `operation_type_id -> operation_types.id`; `user_id -> users.id`; `annulled_by -> users.id` | PK `id`; UNIQUE `idempotency_key`; INDEX (`user_id`, `effective_at`); INDEX (`bank_agent_id`, `effective_at`); INDEX (`store_id`, `effective_at`); INDEX (`operation_type_id`, `effective_at`); INDEX (`status`, `effective_at`); INDEX (`organization_id`, `effective_at`) |
| `daily_closures` | `id bigint`, `organization_id bigint`, `store_id bigint`, `bank_agent_id bigint`, `business_date date`, `status varchar(20) default ACTIVO`, `operation_count unsigned int default 0`, `gross_amount decimal(18,2) default 0`, `cash_in decimal(18,2) default 0`, `cash_out decimal(18,2) default 0`, `net_movement decimal(18,2) default 0`, `has_pending_confirm boolean default false`, `confirmed_by bigint nullable`, `confirmed_at datetime(6) nullable`, `reopened_by bigint nullable`, `reopened_at datetime(6) nullable`, `reopen_reason varchar(500) nullable`, `created_at datetime(6)`, `updated_at datetime(6)` | `organization_id -> organizations.id`; `store_id -> stores.id`; `bank_agent_id -> bank_agents.id`; `confirmed_by -> users.id`; `reopened_by -> users.id` | PK `id`; UNIQUE (`bank_agent_id`, `business_date`, `status`); INDEX (`bank_agent_id`, `business_date`); INDEX (`organization_id`, `business_date`); INDEX `status` |
| `daily_closure_operations` | `id bigint`, `daily_closure_id bigint`, `operation_id bigint`, `created_at datetime(6)` | `daily_closure_id -> daily_closures.id`; `operation_id -> operations.id` | PK `id`; UNIQUE `operation_id`; INDEX `daily_closure_id` |

Todas las tablas salvo `session_events`, `audit_logs` y `daily_closure_operations` tienen el par `created_at`/`updated_at`; esas tres administran timestamps manualmente y no tienen `updated_at`.
